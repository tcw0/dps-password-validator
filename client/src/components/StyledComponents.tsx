import { TextField, styled } from "@mui/material"

export const StyledTextfield = styled(TextField)({
  input: { color: "white" },
  fieldset: {
    borderColor: "white",
  },
  label: { color: "white" },
})
